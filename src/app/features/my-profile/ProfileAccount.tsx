import MainWrapper from '../../components/MainWrapper';

//ADD TRANSLATIONS

const ProfileAccount = () => {
    return (
        <MainWrapper>
            <div>Account</div>
        </MainWrapper>
    );
};

export default ProfileAccount;
